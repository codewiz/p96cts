*.o
/p96cts

# Release archives built by "make release".
/p96cts-*.lha

# Scratch: unpacked upstream tarballs, probe programs, build leftovers.
/tmp

# A run's own images. The references under golden/ are committed -- PNG makes
# them small enough to carry -- but these are regenerated on every run.
/output
